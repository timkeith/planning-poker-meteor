Run:
  meteor run

Mongo
  meteor mongo
    db.BAR.drop()

Deploy
  to meteor.com:
    meteor deploy FOO.meteor.com 
  reset the app completely (the re-deploy):
    meteor deploy FOO.meteor.com --delete
